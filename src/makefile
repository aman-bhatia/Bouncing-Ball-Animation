CXX = g++
CFLAGS = -std=c++11 -g
LFLAGS = -lglui -lglut -lGLU -lGL -lm
SRCS = main.cpp ball.cpp eventHandling.cpp gui.cpp imageloader.cpp tinyfiledialogs.c

all:
	$(CXX) $(CFLAGS) -o a.out $(SRCS) $(LFLAGS)
